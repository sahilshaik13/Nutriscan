[project]
name = "backend"
version = "0.1.1"
description = "Food Nutrition Analyzer API with Gemini"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
]
